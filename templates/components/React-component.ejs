import React from 'react';
import '<%= cssPath %>';

const <%= componentName %> = () => {
  return (
    <>
      <%= templateData.html %>
    </>
  );
};

export default <%= componentName %>;
